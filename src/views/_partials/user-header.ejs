<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title><%= title %></title>
    <style>
      .pagination {
        list-style-type: none;
        padding: 0;
      }
	  
	  a:link {
		color:aqua;
	  }
	  
	  a:visited {
		color:white;
	  }
	  
      .pagination li {
        display: inline-block;
      }
      .pagination li a {
        display: block;
        padding: 0.5em 1em;
        text-decoration: none;
      }
      .pagination li.active a {
        color: #0a0a0a;
      }
      
      table {
        border-collapse: collapse;
        border: 1px solid #ccc;
      }
      table td, table th {
        border: 1px solid #ccc;
        padding: 0.25em 0.5em;
      }
      th.active {
        background-color: #2b2b2b;
      }
      td.actions {
        padding: 0;
        text-align: center;
      }
      td.actions a {
        text-decoration: none;
        padding: 0.5em;
        height: 100%;
        width: 100%;
      }
      td.actions:hover {
        background-color: #fff;
      }
      
      @media (max-width: 600px) {
        table {
          width: 100%;
        }
        table td, table th {
          display: block;
          width: 100%;
        }
      }
    </style>
  </head>
  <body style="font-family: sans-serif;color:white; background-color: #1a1a1a; padding: 1em;">
